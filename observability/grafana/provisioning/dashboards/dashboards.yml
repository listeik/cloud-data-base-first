apiVersion: 1

providers:
  - name: Cloud Data Base
    orgId: 1
    folder: Cloud Data Base
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
